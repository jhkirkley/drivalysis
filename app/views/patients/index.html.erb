<h1>Patients List</h1>
<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <% @patients.each do |patient| %>
      <ul>
        <li> <%= link_to patient.name, patient %>
      </ul>
    <% end %>
  </div>
</div>
